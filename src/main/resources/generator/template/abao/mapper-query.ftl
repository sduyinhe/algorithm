package ${basePackage}.repository.${prefix}.query;

import ${modelPackage}.${prefix}.${modelName};
import com.github.crm.repository.BaseQueryRepository;

/**
* Created by ${author} on ${date}.
*/
public interface ${modelName}QueryMapper extends BaseQueryRepository<Integer,${modelName}> {

}
