package ${basePackage}.repository.${prefix}.command;

import ${modelPackage}.${prefix}.${modelName};
import com.github.crm.repository.BaseCommandRepository;

/**
* Created by ${author} on ${date}.
*/
public interface ${modelName}CommandMapper extends BaseCommandRepository<Integer,${modelName}> {

}
